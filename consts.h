#ifndef CONST_H
#define CONST_H

#include "precision.h"

const int CNT = 2;
const real INTERVAL = 50;
const real MARGIN = 200;

const int WIDTH=800;
const int HEIGHT=600;

const real G = 0;
const real FRICTION = 1;
const real k = 1.f;

const int SUBSTEP = 5;

#endif
